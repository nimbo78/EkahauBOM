# Pre-commit hooks configuration for EkahauBOM
# See https://pre-commit.com for more information

repos:
  # Black - code formatter
  - repo: https://github.com/psf/black
    rev: 24.1.1
    hooks:
      - id: black
        # Use line-length from pyproject.toml (100)

  # Flake8 - linter (disabled for pre-commit, runs in CI/CD)
  # - repo: https://github.com/pycqa/flake8
  #   rev: 7.0.0
  #   hooks:
  #     - id: flake8
  #       args: [--max-line-length=120, --extend-ignore=E203,E501,F401,F841,F541]
  #       exclude: ^(build|dist|\.git|\.venv|venv|__pycache__|\.pytest_cache|tests)/

  # MyPy - type checker (disabled for pre-commit, runs in CI/CD)
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       additional_dependencies:
  #         - types-PyYAML
  #         - types-openpyxl
  #       args: [--ignore-missing-imports, --follow-imports=silent]
  #       exclude: ^(tests|build|dist)/

  # General pre-commit hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        exclude: ^(\.md|\.txt)$
      - id: end-of-file-fixer
        exclude: ^(\.md|\.txt)$
      # check-yaml disabled - too strict for workflow files
      # - id: check-yaml
      #   args: [--safe]
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: check-json
      - id: check-toml
      - id: check-merge-conflict
      - id: debug-statements
      - id: mixed-line-ending
        args: [--fix=lf]

  # Pre-push hook - verify formatting and tests before push
  # This prevents CI/CD failures by catching issues locally
  - repo: local
    hooks:
      - id: pre-push-checks
        name: Pre-push quality checks
        entry: bash .pre-push-hook.sh
        language: system
        stages: [pre-push]
        pass_filenames: false
        always_run: true
        verbose: true

  # Pytest - run quick tests before commit (optional)
  # Uncomment if you want to run tests on every commit
  # WARNING: This will slow down commits significantly
  # - repo: local
  #   hooks:
  #     - id: pytest-quick
  #       name: Quick unit tests
  #       entry: pytest
  #       language: system
  #       pass_filenames: false
  #       always_run: true
  #       args: [tests/, -q, --tb=short, -m, "not slow", -x]
  #       stages: [commit]
