# ==============================================================================
# Ekahau BOM Web - Configuration File
# ==============================================================================
# Copy this file to .env and update the values for your environment
# IMPORTANT: Never commit .env file with real credentials to git!
# ==============================================================================

# ------------------------------------------------------------------------------
# Environment
# ------------------------------------------------------------------------------
# Options: development | production
ENVIRONMENT=development

# ------------------------------------------------------------------------------
# Storage Backend Configuration
# ------------------------------------------------------------------------------
# Storage backend type: local | s3
# - local: Store files on local filesystem (default)
# - s3: Store files on S3-compatible storage (AWS S3, MinIO, Wasabi, etc.)
STORAGE_BACKEND=local

# Local Storage Settings (used when STORAGE_BACKEND=local)
PROJECTS_DIR=data/projects
INDEX_FILE=data/index.json

# ------------------------------------------------------------------------------
# S3-Compatible Storage Settings (optional, required if STORAGE_BACKEND=s3)
# ------------------------------------------------------------------------------
# Works with AWS S3, MinIO, Wasabi, DigitalOcean Spaces, Dell EMC ECS, IBM Cloud, etc.
# Uncomment and configure if using S3 storage backend:

# S3 credentials (required for S3 backend)
# S3_ACCESS_KEY=your_access_key_here
# S3_SECRET_KEY=your_secret_key_here

# S3 bucket name (required for S3 backend, bucket must exist)
# S3_BUCKET_NAME=ekahau-bom-projects

# S3 region (provider-specific, default: us-east-1)
# S3_REGION=us-east-1

# S3 endpoint URL (optional, leave empty for AWS S3)
# Examples:
#   - AWS S3: leave empty (default)
#   - MinIO: http://minio.local:9000
#   - Wasabi: https://s3.us-east-1.wasabisys.com
#   - DigitalOcean: https://nyc3.digitaloceanspaces.com
#   - Corporate: https://s3.internal.company.com
# S3_ENDPOINT_URL=

# S3 SSL settings (optional, default: true)
# S3_USE_SSL=true
# S3_VERIFY=true  # Can be false for dev/test (NOT recommended for production)

# S3 custom CA certificate bundle (optional)
# Path to custom CA certificate file for corporate S3 systems
# S3_CA_BUNDLE=/path/to/ca-bundle.crt

# ------------------------------------------------------------------------------
# API Configuration
# ------------------------------------------------------------------------------
API_PREFIX=/api
# Max upload size in bytes (default: 500MB)
MAX_UPLOAD_SIZE=524288000

# ------------------------------------------------------------------------------
# Authentication & Security
# ------------------------------------------------------------------------------
# ⚠️ IMPORTANT: Change these credentials in production!
# Default admin credentials for web UI login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=EkahauAdmin

# JWT Secret Key - MUST be changed in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=CHANGE_ME_IN_PRODUCTION_USE_ENV_VARIABLE

# JWT token expiration time in minutes (default: 480 = 8 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# ------------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# ------------------------------------------------------------------------------
# Allowed origins for API requests
# Development: Allow localhost frontend
CORS_ORIGINS=["http://localhost:4200"]

# Production example (uncomment and modify):
# CORS_ORIGINS=["https://ekahau-bom.yourdomain.com"]

# ------------------------------------------------------------------------------
# Short Links Configuration
# ------------------------------------------------------------------------------
# Length of generated short links (default: 8 characters)
# SHORT_LINK_LENGTH=8

# Short link expiration in days (default: 30 days)
# SHORT_LINK_EXPIRY_DAYS=30
